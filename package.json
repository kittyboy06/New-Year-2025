{
  "name": "new-year-2025-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && node inline.js",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.2.4",
    "vite-plugin-singlefile": "^2.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0"
  }
}