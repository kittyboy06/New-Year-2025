
import { defineConfig } from 'vite'

export default defineConfig({
    // Setting base to './' allows the app to be deployed to any subdirectory
    // (like https://user.github.io/repo-name/) without absolute path issues.
    base: './'
})
